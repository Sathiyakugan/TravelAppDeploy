<div class="alert alert-success" id="messageshow" on="FadeToZero();">
    <% Object.keys(messages).forEach(function (type) { %>
    <ul class="<%= type %>">
        <% messages[type].forEach(function (message) { %>
        <li><%= message %></li>
        <% }) %>
    </ul>
    <% }) %>
</div>